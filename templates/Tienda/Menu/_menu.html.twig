<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('index') }}">Tienda Figther</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        Gestión de Usuario<span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        <li class="divider"></li>
                        <li class="dropdown-toggle"><b>Usuario</b></li>
                        <li class="divider"></li>
                        <li><a href="{{ path('config', {'id':app.session.get('userID') }) }}">Configuracion</a></li>
                    </ul>
                </li>

                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                        Gestión de Skins<span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        <li class="divider"></li>
                        <li class="dropdown-toggle"><b>Skins</b></li>
                        <li class="divider"></li>
                        <li><a href="{{ path('ver') }}">Ver Skins</a></li>
                    </ul>
                </li>
            </ul>
            <form class="navbar-form navbar-left" role="search" method="POST" action="{{ path('ver') }}">
                <div class="form-group">
                    <input class="form-control" placeholder="Nombre Libro" type="text" name="nombre">
                </div>
                <button type="submit" class="btn btn-default">Buscar</button>
            </form>
            <h5>Usuario: {{ app.session.get('nombre') }}</h5>
            <h5>Dinero :{{ app.session.get('dinero') }}</h5>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>